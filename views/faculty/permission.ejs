<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>App Permission</title>
    
    <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css">

    <style>.file {
        border: 1px solid #ccc;
        display: inline-block;
        padding: 6px 12px;
        cursor: pointer;
    }</style>
</head>
<body>
    <header>
        <%- include ("../partials/facultyheader") %>
    </header>
    <h3 class="jumbotron text-center" >Welcome <b><%=user %></b></h3>
    <div class="container" style="width: 300px;height: 400px;text-align: center;">

        <h5 class="lead">Permission Inbox</h5>
        
                
                <table  class="table table-hover">
                    <tr>
                      <th>Sender</th>
                      <th>File</th>
                    </tr>
                  
                    <% for (var i = 0; i < info.length;  i++) { %>
                      <tr>
                        <td><%= data[i].sender %></td>
                        <td><a href="/<%=data[i].file%>"><%= data[i].file %></a></td>
                      </tr>    
                     <% } %>
                </table>
        
    </div>
    <footer>
        <%- include ("../partials/footer") %>
    </footer>
</body>
</html>